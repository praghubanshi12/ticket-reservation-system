import { FlightDepartureTime } from './flight-departure-time';

describe('FlightDepartureTime', () => {
  it('should create an instance', () => {
    expect(new FlightDepartureTime()).toBeTruthy();
  });
});
